#include <stdio.h>

// empty_node();

//Adds an entry (key, value) into the database. 
void add_node(char input_key[], char input_value[]);

//Searches for a matching key in the database and returns whether it is found or not.
int search_entry(char input_buffer[]);

//Changes the value of a specified key in an entry
/* void update_entry(); */

//
/* void insert_entry(); */

//
/* void delete_entry(); */

//
void print_database();
